# High-Impact Personal Portfolio

This portfolio is built with **React, Vite, and Framer Motion**, featuring a modern, premium design as requested.

## 🚀 Getting Started

1.  **Install dependencies** (if you haven't already):
    ```bash
    npm install
    ```

2.  **Start the development server**:
    ```bash
    npm run dev
    ```

3.  **Build for production**:
    ```bash
    npm run build
    ```

## 📄 Resume Configuration

The "Download Resume" button on the Home page points to `/resume.pdf`.
**Action Required:** specificy your resume file:
1.  Place your `resume.pdf` file inside the `public/` folder.
2.  Or update the link in `src/pages/Home.jsx`.

## 🎨 Customization

-   **Colors & Fonts**: Edit `src/index.css` to change CSS variables (colors, fonts).
-   **Content**: Edit the files in `src/pages/` to update text and data.
    -   `Home.jsx`
    -   `About.jsx`
    -   `Experience.jsx`
    -   `Projects.jsx` (Edit `projectsData` array)
    -   `Skills.jsx` (Edit `skillsData` array)
    -   `Contact.jsx`
